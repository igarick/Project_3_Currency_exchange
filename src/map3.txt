✅ 1. Создать заготовку Java-бэкенд-приложения с javax.servlet / jakarta.servlet
📌 Что означает:

Подготовить структуру проекта: папки, web.xml, зависимости.

Настроить Maven или Gradle.

Подключить servlet API (javax или jakarta).

Настроить запуск на локальном сервере (Tomcat или Jetty).

Убедиться, что GET и POST запросы доходят до сервлетов.

📚 Что изучить:

Как устроен Java Web-проект

Что такое сервлеты, и как они обрабатывают HTTP-запросы

Отличие javax.servlet и jakarta.servlet (новая версия, меняется только пакет)

Основы конфигурации web.xml

Что такое WAR-архив

Установка и настройка Tomcat/Jetty

✅ 2. Создать таблицы в базе данных и вручную их заполнить начальными данными
📌 Что означает:

Выбрать СУБД (обычно SQLite для простоты)

Создать 2 таблицы: currencies и exchange_rates

Добавить 3–5 валют (например, USD, EUR, RUB)

Добавить несколько курсов обмена между ними

Проверить, что база подключается и читается

📚 Что изучить:

Основы SQL (DDL и DML)

Работа с SQLite или другой СУБД

Как использовать SQLite GUI (DB Browser for SQLite или TablePlus)

Основы JDBC: как Java работает с базами

✅ 3. Реализовать методы REST API для работы с валютами и обменными курсами
📌 Что означает:

Сделать API для:

Просмотра списка валют (GET /currencies)

Добавления валюты (POST /currencies)

Просмотра курсов (GET /rates)

Добавления/изменения курса (POST /rates)

Подключить JSON-библиотеку (Gson, Jackson или org.json)

Убедиться, что данные читаются/записываются из базы

📚 Что изучить:

REST API: что это, принципы (URL-ы, методы, коды)

HTTP-методы: GET, POST, PUT, DELETE

Формат JSON: чтение и генерация в Java

Структура сервлета: doGet, doPost

Как обрабатывать параметры запроса и тела (request.getParameter, request.getReader)

✅ 4. Реализовать метод REST API с подсчётом обмена валюты
📌 Что означает:

Сделать эндпоинт вроде GET /convert?from=USD&to=EUR&amount=100

Извлечь курс из таблицы exchange_rates

Рассчитать сумму и вернуть её в JSON-формате

Добавить обработку ошибок (валюта не найдена, курс отсутствует и т.п.)

📚 Что изучить:

Как принимать параметры в GET-запросах (?key=value)

Как писать сервисный метод для бизнес-логики (конвертация валют)

Как возвращать корректные HTTP-статусы (200, 400, 404)

Простая валидация параметров (пустые, нечисловые и т.д.)

✅ 5. Деплой на удалённый сервер
📌 Что означает:

Упаковать проект в .war (если Tomcat) или .jar (если embedded сервер)

Перенести файл на удалённый сервер (через SSH, FTP, SCP и т.д.)

Установить и настроить Tomcat на сервере

Разместить war в /webapps (или использовать CI/CD)

Проверить доступ к API по внешнему IP или домену

📚 Что изучить:

Как собирать war-файл с Maven

Как устанавливать и запускать Tomcat на Linux

Основы командной строки (Linux)

Основы работы по SSH

Как проверить серверное приложение (curl, Postman, браузер)

Базовая безопасность (брандмауэр, порты, iptables)

💡 ИТОГОВО
Этап	Цель	Что изучить
1. Заготовка проекта	Подключить сервлеты и запустить сервер	Maven, сервлеты, Tomcat
2. БД и начальные данные	Создать структуру хранения	SQL, SQLite, JDBC
3. CRUD API	Сделать REST API для валют и курсов	REST, JSON, HTTP
4. Конвертация	Сделать основной функционал	Логика сервиса, параметры, валидация
5. Деплой	Опубликовать API онлайн	WAR, Tomcat, Linux-сервер, SSH